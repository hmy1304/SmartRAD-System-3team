user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen 80;
        server_name hospitalmedic.store; # 가비아 도메인으로 변경하세요 (예: smartrad.com)

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name hospitalmedic.store; # 가비아 도메인으로 변경하세요

        ssl_certificate /etc/letsencrypt/live/hospitalmedic.store/fullchain.pem; # 실제 도메인으로 변경
        ssl_certificate_key /etc/letsencrypt/live/hospitalmedic.store/privkey.pem; # 실제 도메인으로 변경

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN";
        add_header X-XSS-Protection "1; mode=block";
        add_header X-Content-Type-Options "nosniff";

        # Route all requests to frontend (Next.js handles both UI and API rewrites)
        location / {
            proxy_pass http://frontend:3000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            
            # Forward real IP to Next.js
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Example for static files caching optimization (if Next.js serves from /_next/static)
        location /_next/static/ {
            proxy_pass http://frontend:3000;
            proxy_cache_valid 200 302 365d;
            proxy_cache_valid 404 1m;
            expires max;
            add_header Cache-Control "public, max-age=31536000, immutable";
        }
    }
}
